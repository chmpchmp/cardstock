An encyclopedic website that tracks the prices of Magic: the Gathering cards as well as other information.

A script produces each webpage for every marketable physical card.
An API that tracks the most recent prices were not found, so a json file is being used as a replacement.
Not every single card's webpage is uploaded, due to GitHub's storage limit.
